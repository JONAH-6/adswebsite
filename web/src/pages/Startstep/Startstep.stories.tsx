import type { Meta, StoryObj } from '@storybook/react'

import Startstep from './startstep'

const meta: Meta<typeof Startstep> = {
  component: Startstep,
}

export default meta

type Story = StoryObj<typeof Startstep>

export const Primary: Story = {}
